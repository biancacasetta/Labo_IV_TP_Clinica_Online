import { FiltroCamposPipe } from './filtro-campos.pipe';

describe('FiltroCamposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCamposPipe();
    expect(pipe).toBeTruthy();
  });
});
