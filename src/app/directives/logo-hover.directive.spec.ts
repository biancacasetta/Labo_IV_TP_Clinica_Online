import { LogoHoverDirective } from './logo-hover.directive';

describe('LogoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
